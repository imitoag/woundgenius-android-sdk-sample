<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name="io.imito.woundgenius.sample.core.SampleWoundSDKApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/sample_app_data_extraction_rules"
        android:fullBackupContent="@xml/sample_app_backup_rules"
        android:icon="@mipmap/sample_app_ic_launcher_round"
        android:label="Imito WoundGenius SDK Sample"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/sample_app_ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme"
        tools:ignore="LockedOrientationActivity">

        <activity
            android:name="io.imito.woundgenius.sample.ui.screen.main.MainActivity"
            android:exported="true"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity android:name=".ui.screen.measurementresult.holder.MeasurementResultHolderActivity" />


        <!--
            The host app keeps the bare androidx.core.content.FileProvider under its own
            authority: the SDK now ships WoundGeniusFileProvider instead, so the two providers
            no longer collide during manifest merge. This mirrors what a real integrator has.
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/sample_app_file_provider_paths" />
        </provider>

    </application>

</manifest>